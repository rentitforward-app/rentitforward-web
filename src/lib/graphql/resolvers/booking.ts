import { GraphQLContext } from '../context';

export const bookingResolvers = {
  Query: {
    // Basic placeholder
  },
  
  Booking: {
    // Field resolvers will be added later
  },
  
  Subscription: {
    // Subscription resolvers will be added later
  },
}; 